# regomoon —— MoonBit 的 Rego 策略语言引擎

## 项目申报书

### 一、基本信息

| 项 | 内容 |
| --- | --- |
| 项目名称 | regomoon |
| 项目类别 | MoonBit 开源库移植 |
| 申报人 | 成欣阳 |
| 联系方式 | 2193772956@qq.com |
| GitHub 账号 | CYang-dep |
| 项目仓库 | https://github.com/CYang-dep/regomoon |
| 上游项目 | Open Policy Agent（OPA）的 Rego 策略语言 |
| 对标上游版本 | OPA v1.20.2（2026-09-03 发布）的 Rego v1 语法与求值语义 |
| 开源许可证 | Apache-2.0（本项目与上游一致） |

需要说明的是：本项目移植的是 **Rego 这门语言**，不是 OPA 的 Go 源码。词法器、解析器、
求值器和内置函数库都依据 Rego 官方语言文档的语义从零实现，代码与 OPA 仓库没有派生关系，
仅在语言行为上对齐。这也决定了后文的对标方式：比的是语言语义，而不是逐行对应。

### 二、项目背景与目标

策略和业务代码混在一起，是后端开发里反复出现的麻烦。典型的样子是：鉴权逻辑散落在
各个 handler 里，`if user.role == "admin"` 这类判断写了十几处；要新增一种角色，得改代码、
重新编译、重新发版；运维想在出口处统一拦一道，只能再写一个中间件。Rego 的思路是把
"谁能在什么条件下对什么做什么"抽成独立的策略文件，策略是数据，改策略不用改程序。

这套做法在 Go 生态里已经很成熟，但要用它，前提是宿主环境里有一个能读懂 `.rego` 文本的
求值器。MoonBit 目前没有这样的东西，所以 MoonBit 应用想用声明式策略，只能自己用代码把
规则硬编码一遍。

regomoon 补的就是这一块：给定一段 Rego 文本和一个 JSON 输入，返回判定结果。它是一个
纯库，没有服务器、没有网络依赖、不绑定任何具体业务，Web 框架的鉴权中间件、网关的
过滤规则、CI 里的配置检查、边缘函数里的轻量判定，都可以直接嵌。

本次交付的范围是 Rego v1 的核心语言面：模块结构（`package` / `import` / `default`）、
四类规则、三种推导式、量词与否定、46 个常用内置函数，以及带行列号的结构化错误。
明确不包含的：OPA 的服务器与 REST API、`with` 修饰符、依赖宿主能力的内置函数
（HTTP 调用、时间、加密等）、以及部分求值等编译期优化。

### 三、移植内容与技术方案

#### 1. 上游依据

语言语法与求值语义依据 OPA 官方文档中的 Rego 语言定义（v1.x），内置函数的名称与行为
对照 OPA 的 Builtin Functions 文档。上游许可证 Apache-2.0，本项目沿用同一许可证并在
`THIRD_PARTY_NOTICES.md` 中注明语言来源。

#### 2. 核心模块

```
value.mbt    值模型：null/bool/number/string/array/set/object
json.mbt     严格 RFC 8259 JSON 读取，供 data 与 input 使用
error.mbt    结构化错误类型
lexer.mbt    词法分析，输出带行列号的 token
ast.mbt      语法树及其回显
parser.mbt   递归下降 + 优先级爬升
builtins.mbt 内置函数库
eval.mbt     规则体求值、规则语义、推导式、运算符
engine.mbt   对外 API
```

数据流是一条线：策略文本经 `lexer` 变成 token 流，`parser` 生成语法树，`engine` 把语法树
连同 `data` / `input` 两份 JSON 文档交给 `eval` 求值，`eval` 算出的值仍是 `value` 里的
Rego 值，可以原样序列化回 JSON。

#### 3. MoonBit 适配

有三处设计与上游实现思路不同，都是因为宿主语言不同：

**值模型用代数数据类型。** Rego 的值比 JSON 多一个集合类型，本项目用 `enum` 表达七种值，
并保证集合与对象始终处于排序去重的规范形态。这样做的好处是相等判断、排序和输出都是
确定性的，不用在比较时再处理无序问题。

**规则体求值用回溯搜索。** Rego 的规则体是合取式，`some x in xs` 会引入多条可行绑定，
所以求值一个规则体得到的是"所有满足约束的变量环境"的集合，而不是单个布尔值。`eval.mbt`
按语句逐条推进，每条语句可能把环境分裂成多支，最终返回全部存活的分支。

**错误用统一的 `RegoError` 结构。** 十类错误（词法、语法、JSON、类型、未定义、参数个数、
除零、冲突、安全性、编译），词法与语法错误带行列号。MoonBit 有代数数据类型和 `Result`，
错误分类可以做得比 Go 版更细，调用方按类别处理而不是解析错误字符串。

依赖只有一个：`moonbitlang/regexp@0.3.5`（Apache-2.0），用于 `regex.match`。
其余全部基于 MoonBit 标准库，因此 `wasm`、`wasm-gc`、`js`、`native` 四个后端都能编译。

#### 4. 兼容边界

与上游一致的部分：规则四种形态（完整、部分集合、部分对象、带参函数规则）、`default`、
三种推导式、`some` / `every` / `not`、集合代数与 `in`、六档运算符优先级、undefined 与
false 的区分。同时兼容 Rego v1 的 `if` / `contains` 写法和旧的 `rule { }` 写法。

子集实现的部分：内置函数只做了 46 个常用的（OPA 的内置函数集远大于此，还包含时间、
加密、HTTP 调用、图遍历等类别）；数字用 `Double`，不是 OPA 的任意精度十进制，超过 2^53
会丢精度。

暂不支持的部分：`with` 修饰符（遇到时报编译错误，而不是含糊的语法错误）、未绑定下标
变量的隐式迭代（需要显式写 `some x in data.arr`）、非字符串对象键、`walk`、点号规则头名。
这些在 README 里逐条列了原因和替代写法。

### 四、典型使用示例

三个示例都在仓库 `examples/` 下，可直接 `moon run` 运行，下列输出均为实际运行结果。

#### 示例一：接口鉴权（`examples/quickstart`）

- 使用者与需求：Web 框架作者想在中间件里做一次"这个请求放不放行"的判断，策略放在配置里。
- 输入或配置：

```rego
package authz

default allow := false

allow if {
  input.action == "read"
  input.object == "data1"
}
```

- 核心调用：

```moonbit
let policy = @regomoon.Policy::compile(SOURCE)
let bound = policy.with_input_json("{\"action\":\"read\",\"object\":\"data1\"}")
bound.allow("allow")
```

- 预期结果：`true`；把 `action` 换成 `write` 则返回 `false`（走 `default` 规则）。
- 实际解决的问题：放行条件写在策略文本里，改规则不用改代码、不用重新编译。

#### 示例二：数据驱动角色（`examples/rbac`）

- 使用者与需求：多租户系统里角色和权限存在数据库里，希望授权逻辑只依赖数据、不依赖代码。
- 输入或配置：

```json
{"roles": {"alice": ["admin"], "bob": ["viewer"]},
 "grants": {"admin": [{"action": "read", "resource": "data1"},
                      {"action": "write", "resource": "data1"}]}}
```

- 核心调用：

```moonbit
bound.query("roles_for(\"alice\")")          // ["admin"]
bound.query("granted(\"admin\", \"write\", \"data1\")")  // true
bound.allow("allow")                          // 按请求判定
```

- 预期结果：alice 对 data1 的写操作返回 `ALLOW`；没有角色的 carol 返回 `DENY`，且
  `deny` 集合给出原因 `{"user has no roles"}`。
- 实际解决的问题：角色与权限全在 `data` 文档里，加一个角色只改数据。`deny` 是部分集合
  规则，多个原因会自动累积成一个集合，适合直接返回给前端。

#### 示例三：配置审计（`examples/audit`）

- 使用者与需求：平台方要批量检查一批服务账号的权限是否符合规范，并输出每个账号的问题数。
- 输入或配置：

```json
{"accounts": [
  {"name": "ci-bot",     "permissions": ["read", "list"]},
  {"name": "deploy-bot", "permissions": ["read", "secret:rotate"]},
  {"name": "legacy-job", "permissions": ["read", "write", "delete"]}]}
```

- 核心调用：

```moonbit
bound.query("violations")       // 所有越权项
bound.query("report")           // 每个账号的越权计数
bound.allow("compliant")
```

- 预期结果（实际运行输出）：

```
compliant      = false
violations     = {{"account": "deploy-bot", "permission": "secret:rotate"},
                  {"account": "legacy-job", "permission": "delete"},
                  {"account": "legacy-job", "permission": "write"}}
needs_approval = {"deploy-bot"}
report         = {"ci-bot": 0, "deploy-bot": 1, "legacy-job": 2}
```

- 实际解决的问题：一个策略文件同时给出明细和汇总。这里用到了推导式捕获外层绑定、
  `not ... in ...`、正则匹配和部分对象规则，是三个示例里语言特性最全的一个。

### 五、对标与质量验证

#### 1. 功能对标

| 能力 | OPA / Rego | 本项目当前状态 | 兼容边界或验证方式 |
|---|---|---|---|
| 模块结构 `package` / `import as` / `default` | 支持 | 已实现 | `parser_test` 覆盖 |
| 完整 / 部分集合 / 部分对象规则 | 支持 | 已实现 | `engine_test` 含三类规则的端到端用例 |
| 带参规则（函数规则） | 支持 | 已实现 | `engine_test` 中 `is_admin` / `roles_for` |
| 三种推导式 | 支持 | 已实现 | `engine_test` 数组/集合/对象各一 |
| `some` / `every` / `not` | 支持 | 已实现 | `engine_test` 存在与全称量词对照用例 |
| 集合代数 `|` `&` `-` 与 `in` | 支持 | 已实现 | `engine_test` 运算符用例 |
| 内置函数 | 全量（含时间、加密、HTTP、图遍历等） | 部分支持，46 个 | `builtins_test` 逐个断言 |
| 正则 | Go `regexp` 语法 | 已实现，改用 `moonbitlang/regexp` | 语法子集，见依赖文档 |
| 数字 | 任意精度十进制 | 部分支持，`Double` | 超过 2^53 丢精度 |
| `with` 修饰符 | 支持 | 未实现 | 显式报编译错误 |
| 隐式迭代 | 支持 | 未实现 | 需显式 `some x in xs` |
| 非字符串对象键 | 支持 | 未实现 | 对象键限定为字符串 |
| `walk`、`http.send`、`opa.runtime` 等宿主内置 | 支持 | 未实现 | 需要宿主能力 |
| 部分求值等编译期优化 | 支持 | 未实现 | 不在本次范围 |

#### 2. 可复现的量化结果

- **核心实现代码量**：引擎 9 个文件共 5,120 行（剔除空行与注释后 4,424 行），不含测试、
  示例与生成文件。测试 6 个文件 1,120 行，示例 3 个共 283 行。
- **测试结果**：`moon test --target wasm` / `wasm-gc` / `js` 三个后端各 **75 通过 / 0 失败**，
  命令为 `moon test --target <t> --deny-warn`。
- **后端验证**：`moon check --target all --deny-warn` 在四个后端全部通过（零警告）。
  工具链为 moon 0.1.20260713 / moonc v0.10.4+2cc641edf。`native` 后端的测试运行需要系统
  C 编译器，本机未安装，已交由仓库 CI 在 ubuntu 上执行。
- **上游对照测试**：未系统性移植 OPA 官方测试集。现有 75 个用例为手工编写，语义对照
  官方文档与常见策略写法，其中 ACL、RBAC、配置审计三类按真实策略形态组织。
  这是当前对标深度上的主要欠缺，见后续计划。
- **覆盖率**：待验证。尚未跑 `moon coverage`。
- **性能测试**：待验证。目前没有与 OPA 或其它实现的对比数据，不做性能方面的结论。

### 六、当前进展与后续计划

**已完成**：语言核心（词法、语法、求值）、46 个内置函数、公共 API、三个可运行示例、
README、Apache-2.0 许可证与第三方声明、CI 配置。代码已推送至 GitHub。

**尚未完成**：`with` 修饰符；隐式迭代；任意精度数字；剩余内置函数（时间、编码、图遍历
等类别）；OPA 官方测试集的系统性移植；覆盖率与性能数据。

**后续计划**（按优先级）：

1. 移植 OPA 官方测试用例中与已实现语言面对应的部分，把"语义对齐"从人工对照变成可执行断言；
2. 支持 `with` 修饰符的 `input` / `data` 覆盖；
3. 补齐字符串与编解码类内置函数；
4. 用 `moon coverage` 取得覆盖率数据，并做一次与 OPA 的同输入耗时对比；
5. 视需求决定是否发布到 mooncakes.io。

**交付内容**：源码（`*.mbt`）、75 个测试用例、3 个可运行示例、README（含语言面对照表与
兼容边界说明）、Apache-2.0 LICENSE、THIRD_PARTY_NOTICES、GitHub Actions CI。

### 七、项目价值

对写 MoonBit 应用的人来说，接入声明式策略通常意味着自己实现一套规则解析加求值，
这是一大块与业务无关的工作。regomoon 把这件事做成一个依赖：策略文本进、判定结果出，
应用只需要决定策略放在哪里。

能直接复用的场景包括 Web 框架的鉴权中间件、API 网关的过滤规则、CI 里的配置合规检查、
以及多租户系统里按租户下发策略。这些场景的共同点是规则经常变、且希望由非写代码的人维护，
正是声明式策略的适用范围。

它是一个通用的语言求值器，不依赖任何具体框架或业务模型，`wasm` 目标也让它可以在浏览器
或边缘环境里跑。

当前的主要限制是内置函数只有 46 个、不支持 `with` 与隐式迭代、数字精度受 `Double` 限制。
维护方向以补齐内置函数和移植上游测试集为主，这两项都不改变现有 API。
